import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const a=readFileSync('lib/modules/assembly.ts','utf8'),c=readFileSync('lib/modules/certification-authority.ts','utf8');assert.doesNotMatch(a,/EA_STRICT_LEGO_CERTIFICATION/);assert.match(a,/missing-10-class-certificate/);assert.match(a,/CHASSIS_STANDARD_MODULE_IDS/);assert.match(c,/exactVersion/);assert.match(c,/certificate\?\.status==='certified'&&exact/);console.log('Strict Lego authority: PASS');