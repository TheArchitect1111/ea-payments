import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const l=JSON.parse(readFileSync('config/module-certification-evidence.json','utf8'));const ids=l.certificates.map(x=>x.moduleId);for(const id of ['billing','events','intake','applications','reports']){const c=l.certificates.find(x=>x.moduleId===id);assert.ok(c,id+' certificate required');assert.equal(c.status,'certified');assert.equal(Object.keys(c.evidence).length,10);}console.log('Lego evidence wave 1 promoted through strict authority: PASS');