name: EA Payments Hourly Production Guard

on:
  schedule:
    - cron: '37 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: ea-payments-hourly-production-guard
  cancel-in-progress: true

jobs:
  guard:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Verify production availability
        shell: bash
        run: |
          set -euo pipefail
          URL='https://ea-payments.vercel.app/'
          BODY="$(mktemp)"
          CODE="$(curl -L -sS --max-time 30 -o "$BODY" -w '%{http_code}' "$URL")"
          test "$CODE" = '200'
          test "$(wc -c < "$BODY")" -gt 500

      - name: Open or update incident
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const title='[Production Guard] EA payments outage detected';
            const body=`Hourly production guard failed at ${new Date().toISOString()}. Run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const {data:issues}=await github.rest.issues.listForRepo({owner:context.repo.owner,repo:context.repo.repo,state:'open',per_page:100});
            const existing=issues.find(i=>i.title===title);
            if(existing) await github.rest.issues.createComment({owner:context.repo.owner,repo:context.repo.repo,issue_number:existing.number,body});
            else await github.rest.issues.create({owner:context.repo.owner,repo:context.repo.repo,title,body});
