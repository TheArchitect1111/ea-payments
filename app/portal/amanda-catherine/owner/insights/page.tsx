import { getAmandaOwnerInsights } from '@/lib/amanda-catherine/insights';
export default async function InsightsPage(){const data=await getAmandaOwnerInsights();const metrics=[['Academy courses',data.academy.activeCourseCount],['Students',data.academy.studentCount],['Certificates',data.academy.certificateCount],['Progress records',data.academy.progressRecordCount]];return <div className="ac-dashboard"><header className="ac-topbar"><div><small>BUSINESS VISIBILITY</small><h1>Business Insights</h1><p>Verified operational signals first. Portal V2 will never manufacture revenue, appointment or conversion numbers when a live source has not been verified.</p></div><div className="ac-status">Source-aware reporting</div></header><section className="ac-metrics">{metrics.map(([label,value])=><div className="ac-metric" key={String(label)}><strong>{value}</strong><span>{label}</span></div>)}</section><section className="ac-card" style={{marginTop:20}}><span className="ac-eyebrow">REPORTING MODEL</span><h3>Available report lanes</h3><div className="ac-course-list">{data.reports.map(report=><div key={report}><strong>{report.replaceAll('-',' ')}</strong><span>EA model</span></div>)}</div></section><section className="ac-card" style={{marginTop:20}}><span className="ac-eyebrow">WAITING FOR VERIFIED LIVE SOURCE</span><p>{data.unavailableWithoutVerifiedSource.join(' · ')}</p></section></div>;}