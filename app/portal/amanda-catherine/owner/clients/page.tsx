import { getAmandaAcademyOperations } from '@/lib/amanda-catherine/owner-operations';
export default async function ClientsPage(){const academy=await getAmandaAcademyOperations();return <div className="ac-dashboard"><header className="ac-topbar"><div><small>CLIENTS · ACADEMY ACCESS</small><h1>Clients</h1><p>People with Amanda Academy activity are surfaced from the existing access and learning records. No duplicate client database is created.</p></div><div className="ac-status">{academy.students.length} active learners</div></header><section className="ac-card"><span className="ac-eyebrow">ACADEMY ROSTER</span><h3>Students & enrollments</h3>{academy.students.length?<div className="ac-course-list">{academy.students.map(student=><div key={student.email}><strong>{student.email}<small>{student.courses.join(' · ')||'Access provisioned'}</small></strong><span>{student.certificates} cert{student.certificates===1?'':'s'}</span></div>)}</div>:<div className="ac-empty"><p>No persisted Academy learner activity is available yet.</p></div>}</section></div>}