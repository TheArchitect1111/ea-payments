import { AMANDA_DOCUMENT_OPERATIONS as docs, getAmandaAcademyOperations } from '@/lib/amanda-catherine/owner-operations';
export default async function DocumentsPage(){const academy=await getAmandaAcademyOperations();const certs=academy.progress.filter(item=>item.certificateIssuedAt);return <div className="ac-dashboard"><header className="ac-topbar"><div><small>LIBRARY · EXISTING DELIVERY LAYER</small><h1>Documents & Certifications</h1><p>Private deliveries and course completion records remain in EA’s existing stores. Portal V2 surfaces them without creating duplicate storage.</p></div><div className="ac-status">{academy.certificateCount} certificates</div></header><section className="ac-card"><span className="ac-eyebrow">CERTIFICATE RECORDS</span><h3>{certs.length?'Issued certifications':'No issued certifications yet'}</h3>{certs.length?<div className="ac-course-list">{certs.map(item=><div key={`${item.email}-${item.courseId}`}><strong>{item.email}<small>{item.courseId}</small></strong><span>{item.certificateIssuedAt?.slice(0,10)}</span></div>)}</div>:<div className="ac-empty"><p>Certificates will appear here from verified course completion records.</p></div>}</section><section className="ac-card" style={{marginTop:20}}><span className="ac-eyebrow">CONNECTED SOURCES</span><div className="ac-course-list">{docs.sources.map(source=><div key={source}><strong>{source}</strong><span>Connected</span></div>)}</div></section></div>}