'use client';
import {useMemo,useState} from 'react';import Image from 'next/image';import Link from 'next/link';import '../amplifi-create.css';
type Post={title:string;caption:string;callToAction:string;imageDirection:string;imageUrl?:string};type Campaign={title:string;strategy:string;posts:Post[]};type InitialBrief={goal?:string;topic?:string;audience?:string;details?:string;offer?:string};
const roles=['GET ATTENTION','MAKE THE PROBLEM FAMILIAR','GIVE A REASON TO ACT','BUILD BELIEF','MAKE THE NEXT STEP EASY'];
export default function AmplifiCreateStudio({initialBrief}:{initialMode?:'post'|'series'|'campaign';initialBrief?:InitialBrief}){const [goal,setGoal]=useState(initialBrief?.goal||initialBrief?.topic||'');const [audience,setAudience]=useState(initialBrief?.audience||'');const [details,setDetails]=useState(initialBrief?.details||initialBrief?.offer||'');const [campaign,setCampaign]=useState<Campaign|null>(null);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [email,setEmail]=useState('');const ready=useMemo(()=>goal.trim().length>15&&audience.trim().length>3,[goal,audience]);async function generate(){if(!ready){setError('Give Amplifi a little more detail about the goal and who you want to reach.');return}setBusy(true);setError('');try{const r=await fetch('/api/portal/amplifi/create-campaign',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({promotion:goal,audience,result:goal,details,callToAction:'Take the next step',tone:'Warm, clear, confident guide',imageStyle:'Premium emotionally relevant campaign creative',platforms:['Facebook','Instagram']})});const d=await r.json();if(!r.ok||!d.campaign)throw new Error(d.error||'Amplifi could not build the preview.');setCampaign(d.campaign)}catch(e){setError(e instanceof Error?e.message:'Amplifi could not create this right now.')}finally{setBusy(false)}}return <main className="af-create-shell"><header className="af-create-header"><a href="/amplifi"><Image src="/amplifi/amplifi-logo-premium.png" alt="Amplifi" width={1973} height={797}/></a><span>Campaign Preview</span></header>{!campaign?<><section className="af-create-intro"><small>START WITH THE OUTCOME</small><h1>What would you like Amplifi to help make happen?</h1><p>Be specific. Amplifi will turn the goal into the campaign strategy.</p></section><section className="af-guided-card"><div className="af-question"><label>1. What do you want to change?</label><p>Include the business, the problem and a measurable result if you have one.</p><textarea value={goal} onChange={e=>setGoal(e.target.value)} placeholder="I own a barbershop in Charlotte. Tuesdays and Wednesdays are slow. I want to add 15 appointments a week."/></div><div className="af-question"><label>2. Who do you want to reach?</label><p>Describe the people you want to respond.</p><textarea value={audience} onChange={e=>setAudience(e.target.value)} placeholder="Professionals within 10 miles who value convenience and can book online."/></div><div className="af-question"><label>3. What should Amplifi use?</label><p>Optional. Add what makes you worth choosing, an offer, location, booking link, date or other useful detail.</p><textarea value={details} onChange={e=>setDetails(e.target.value)} placeholder="Easy online booking, strong reviews, premium service and open midweek availability."/></div>{error&&<p className="af-create-error">{error}</p>}<button className="af-build-button" onClick={generate} disabled={busy}>{busy?'Building the campaign…':'Create my campaign preview'}</button><p className="af-approval-note">Free preview · No payment · Nothing publishes</p></section></>:<section className="af-output"><div className="af-preview-status"><b>CAMPAIGN PREVIEW</b><span>Nothing has been published</span></div><small>AMPLIFI BUILT THIS AROUND YOUR GOAL</small><h2>{campaign.title}</h2><p className="af-strategy">{campaign.strategy}</p><div className="af-campaign-map"><div><small>YOUR GOAL</small><strong>{goal}</strong></div><div><small>WHO WE'RE TRYING TO MOVE</small><strong>{audience}</strong></div><div><small>THE CAMPAIGN</small><strong>Five coordinated moves, one objective.</strong></div></div><div className="af-sequence-title"><small>THE CAMPAIGN SEQUENCE</small><h2>Not five random posts.<br/>One idea moving toward action.</h2></div><div className="af-output-grid af-visual-grid">{campaign.posts.map((p,i)=><article key={i}><div className="af-role">{String(i+1).padStart(2,'0')} · {roles[i]}</div><img className="af-creative-preview" src={p.imageUrl||''} alt={p.title}/><h3>{p.title}</h3><p>{p.caption}</p>{i===4&&<b>{p.callToAction}</b>}</article>)}</div><div className="af-deliverables"><div><small>WHAT A FULL CAMPAIGN CAN INCLUDE</small><h3>Graphics. Carousel. Story/Reel direction. Captions. Calls to action.</h3></div><p>This preview shows the campaign idea and sequence. Continue with Amplifi to build the finished assets, review them and prepare the campaign for publishing.</p></div><div className="af-preview-buy"><small>WANT TO BRING THIS CAMPAIGN TO LIFE?</small><h2>Keep the idea. Build the finished campaign.</h2><p>Amplifi can turn this direction into coordinated creative and help you keep the campaign moving.</p><div className="af-email-row"><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your email address"/><Link className="af-build-button" href={`/amplifi/pricing${email?`?email=${encodeURIComponent(email)}`:''}`}>Continue with Amplifi</Link></div><button className="af-text-button" onClick={()=>setCampaign(null)}>Try a different goal</button></div></section>}</main>}
