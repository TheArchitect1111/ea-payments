import { NextRequest, NextResponse } from 'next/server';
import { getStripe } from '@/lib/stripe';
export const dynamic='force-dynamic';
export async function GET(req:NextRequest){if(!process.env.STRIPE_SECRET_KEY)return NextResponse.json({ok:false,paid:false,error:'Stripe not configured'},{status:503});const sessionId=String(req.nextUrl.searchParams.get('session_id')||'');if(!sessionId.startsWith('cs_'))return NextResponse.json({ok:false,paid:false,error:'Invalid session'},{status:400});try{const stripe=getStripe(),session=await stripe.checkout.sessions.retrieve(sessionId),meta=session.metadata||{};const offerOk=String(meta.commerceOfferId||meta.packageId||'')==='twin_city_kappas_deposit',recordOk=String(meta.contractRecordId||'')==='twin-city-kappas-2026-001',amountOk=(session.amount_total||0)===25000&&String(session.currency||'').toLowerCase()==='usd',paid=session.payment_status==='paid'&&offerOk&&recordOk&&amountOk;return NextResponse.json({ok:true,paid,sessionId:session.id,amountTotal:session.amount_total||0,currency:String(session.currency||'').toLowerCase(),offerId:String(meta.commerceOfferId||meta.packageId||''),contractRecordId:String(meta.contractRecordId||''),customerEmail:session.customer_details?.email||session.customer_email||'',verifiedBy:'stripe-server-api'},{headers:{'Cache-Control':'no-store'}});}catch(error){console.error('[twin-city-payment] verification failed',error);return NextResponse.json({ok:false,paid:false,error:'Unable to verify Stripe session'},{status:400});}}