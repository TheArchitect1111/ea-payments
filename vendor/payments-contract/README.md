# @ea/payments-contract

Canonical commerce offers for EA Platform: Stripe offer ids ? module entitlements + display/fulfillment metadata.

Not a payments engine ? Stripe/Airtable stay in ea-payments. This package is the shared contract.
