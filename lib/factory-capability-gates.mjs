/** Pure canRun gates for implemented capabilities. */
function hasOutput(context,kind){return(context.outputs||[]).some((item)=>item.kind===kind);}
function latestKind(context,kind){const items=(context.outputs||[]).filter((item)=>item.kind===kind);return items.length?items[items.length-1]:null;}
export function intakeCanRun(context){const status=context.pipelineStatus;if(status==='CANCELLED'||status==='FAILED')return false;const existingIntake=latestKind(context,'intake');if(existingIntake&&(status==='INTAKE_COMPLETE'||status==='RESEARCHING'))return false;if(['RESEARCHING','DISCOVERING','PLANNING','BUILDING','QA','PUBLISHING','UNDER_REVIEW','PUBLISHED','COMPLETE'].includes(status))return false;if(status==='GENERATING')return !existingIntake;return status==='QUEUED'||status==='INTAKE';}
export function researchCanRun(context){const status=context.pipelineStatus;if(status==='CANCELLED'||status==='FAILED'||hasOutput(context,'research'))return false;if(['DISCOVERING','PLANNING','BUILDING','QA','PUBLISHING','UNDER_REVIEW','PUBLISHED','COMPLETE'].includes(status))return false;return status==='INTAKE_COMPLETE'||status==='RESEARCHING';}
function hasResearchArtifacts(context){return(context.artifacts||[]).some((item)=>item.provenance?.capabilityId==='research'||['website','organization','document','branding','metadata'].includes(item.kind));}
export function discoveryCanRun(context){const status=context.pipelineStatus;if(status==='CANCELLED'||status==='FAILED'||hasOutput(context,'discovery'))return false;return status==='RESEARCHING'&&hasOutput(context,'research')&&hasResearchArtifacts(context);}
function hasDiscoveryArtifacts(context){return(context.artifacts||[]).some((item)=>item.provenance?.capabilityId==='discovery'||['organization_profile','programs','services','audience_segments','content_inventory','technology_stack','learning_opportunities','accessibility_findings','automation_opportunities','recommendations'].includes(item.kind));}
export function planningCanRun(context){const status=context.pipelineStatus;if(status==='CANCELLED'||status==='FAILED'||hasOutput(context,'planning'))return false;return status==='DISCOVERING'&&hasOutput(context,'discovery')&&hasDiscoveryArtifacts(context);}
const BUILDABLE_TYPES=new Set(['website','portal','learning','content','report']);
function pendingBuildableWorkOrders(context){const latestById=new Map();for(const item of context.artifacts||[]){if(item.kind!=='work_order')continue;const wo=item.data?.workOrder;if(!wo?.id)continue;const prev=latestById.get(wo.id);if(!prev||String(wo.createdAt||'')>=String(prev.createdAt||''))latestById.set(wo.id,wo);}return[...latestById.values()].filter((wo)=>BUILDABLE_TYPES.has(wo.type)&&wo.status!=='complete'&&wo.status!=='cancelled');}
export function productionCanRun(context){const status=context.pipelineStatus;if(status==='CANCELLED'||status==='FAILED')return false;if(status!=='PLANNING'&&status!=='BUILDING')return false;if(!hasOutput(context,'planning'))return false;return pendingBuildableWorkOrders(context).length>0;}
export function qaCanRun(context){const status=context.pipelineStatus;if(status==='CANCELLED'||status==='FAILED'||hasOutput(context,'qa'))return false;if(status!=='BUILDING'||!hasOutput(context,'production'))return false;if(pendingBuildableWorkOrders(context).length>0)return false;const production=latestKind(context,'production');if((production?.payload?.metrics?.workOrdersPending??0)>0)return false;const artifacts=context.artifacts||[];const hasDeliverable=artifacts.some((item)=>item.kind==='deliverable');const hasWebsite=artifacts.some((item)=>item.kind==='website_site');const hasConcepts=artifacts.some((item)=>item.kind==='experience_concepts');return hasDeliverable&&(!hasWebsite||hasConcepts);}
export function publishingCanRun(context){const status=context.pipelineStatus;if(status==='CANCELLED'||status==='FAILED'||hasOutput(context,'publishing'))return false;const qa=latestKind(context,'qa');return status==='QA'&&Boolean(qa?.payload?.passed===true&&qa?.payload?.reviewReady===true);}
export function notificationCanRun(context){const status=context.pipelineStatus;if(status==='CANCELLED'||status==='FAILED'||hasOutput(context,'notification'))return false;const publishing=latestKind(context,'publishing');return status==='UNDER_REVIEW'&&Boolean(publishing?.payload?.verified===true&&publishing?.payload?.reviewReady===true);}
