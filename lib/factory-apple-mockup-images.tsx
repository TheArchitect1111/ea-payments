/**
 * Back-compat shim — concept renders live in factory-concept-renders.tsx.
 */
export {
  renderAllAppleMockupPngs,
  renderAllConceptPngs,
  renderAppleLandingPng,
  renderAppleMemberPng,
  renderApplePortalPng,
  renderMemberConceptPng,
  renderPortalConceptPng,
  renderWebsiteConceptPng,
  type AppleMockupBrand,
  type ConceptRenderBrand,
} from '@/lib/factory-concept-renders';
